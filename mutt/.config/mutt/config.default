unmailboxes *
mailboxes !

unsubscribe *

# Update the status bar with the new value of the $my_account variable
set status_format = "    %r%m messages%?n? (%n new)?%?d? (%d to delete)?%?t? (%t tagged)? %?p?(%p postponed)? %> Account: $my_account    "

# vim: ft=muttrc
