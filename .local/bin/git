#!/bin/sh

PATH=/usr/local/bin:/usr/bin

case "$*" in
'-C '* | *' -C '*)
	C=1
	;;
esac

if [ "$PWD" = "$HOME" ] && [ -z "$C" ]; then
	command git --git-dir="$HOME/.dotfiles" "$@"
else
	command git "$@"
fi
